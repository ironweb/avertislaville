<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="viewport" content="width=device-width,user-scalable=no">

  <title><%= t('app_title') %></title>

  <link rel="apple-touch-icon-precomposed" href="/img/touch-icon.png" />
  <%= stylesheet_link_tag "application", :media => "all" %>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
    <script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false"></script>
  <%= javascript_include_tag "application" %>

  <script type="text/javascript">
    // Apple wizardry to remove letterbox
    if (window.screen.height == 568) {
      document.querySelector("meta[name=viewport]").content="width=320.1";
    }
  </script>
</head>
<body class="controller-<%= params[:controller] %>">


<header class="navBar shadow">
  <div><%= link_to(t('nav.back'), content_for(:back_url), :class => 'navigation') %></div>
  <h1><%= content_for(:header_title) %></h1>
  <div></div>
</header>

  <% [:alert, :error, :notice].each do |key| %>
    <% if flash[key] %>
      <div class="flash flash-<%= key %>" data-alert="alert"><%= flash[key] %></div>
    <% end %>
  <% end %>

  <%= yield %>

</body>
</html>
